# Node (kept in case this project ever grows a JS piece)
node_modules/
npm-debug.log*

# Python
__pycache__/
*.pyc
venv/
.venv/

# App data - the whole point is that this stays local and per-machine
data/*.db
data/*.db-journal
backups/
logs/*.log

# Env / secrets
.env
.env.*

# OS / editor
.vscode/
.DS_Store
Thumbs.db

# Output
*.log
