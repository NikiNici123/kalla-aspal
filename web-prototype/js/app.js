// Kalla Aspal - LPSE Monitor: web-prototype placeholder script.
//
// This file does nothing yet. It exists so index.html has somewhere to
// point a <script> tag if this scaffold is ever built out for real - see
// ../README.md for what that would actually involve (a backend API layer
// is required before this file could do anything meaningful, since a
// plain browser script can't read the SQLite database or run the scraper
// directly).
//
// NOT used by the real app - run_app.bat / app.py never load this file.

console.log(
  "Kalla Aspal web-prototype: static scaffold only, not connected to any " +
  "real data. See web-prototype/README.md."
);
